# إعدادات القفل عند النوم

general {
  lock_cmd = hyprlock                
  before_sleep_cmd = hyprlock
  after_sleep_cmd = hyprctl dispatch dpms on
}

#
# إطفاء الشاشة بعد 10 دقائق بدون قفل
listener {
  timeout = 600
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on
}

